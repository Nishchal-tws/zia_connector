{
  "installCommand": "git submodule update --init --recursive && cd frontend/zia && npm install",
  "buildCommand": "cd frontend/zia && npm run build",
  "outputDirectory": "frontend/zia/build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}


{
  "installCommand": "git submodule update --init --recursive",
  "buildCommand": "cd frontend/zia && npm install && npm run build",
  "outputDirectory": "frontend/zia/build",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

